//
// Created by Omeed on 8/20/26.
//

#include "mcu.h"
#include <stdlib.h>
/*
component* init_nrf52840() {
    component* c = malloc(sizeof(component));
    c->type = COMPONENT_NRF52840;
    nrf52840* mcu = malloc(sizeof(nrf52840));
    c->impl = mcu;
    pin *pins = (pin*)&mcu;
    for (int i = 0; i < sizeof(nrf52840)/sizeof(pin); i++) {
        pins[i].parent = c;
    }
    return c;
}*/

